# Setup Retro — 2026-06-05

Produced by `/improve-setup`. Evidence reviewed: 6 session-log entries since
the previous retro, `git log --oneline -15`, all three big-rock plans,
`CLAUDE.md`, and the three domain skills.

## Observations

1. **Fiscal calendar re-explained three times.** Sessions on 2026-05-21,
   2026-05-27, and 2026-06-02 each began with the user correcting FY26
   boundaries while discussing scorecard figures. A convention this load-bearing
   shouldn't depend on the user remembering to restate it.
2. **Attribution weights defined in two places.** The canonical weight table
   existed in both `attribution-compare/SKILL.md` and the attribution plan,
   and they had already drifted (Time Decay half-life: 30 vs. 45 days).
   Duplicated definitions are how attribution disputes start.
3. **Recurring quota questions with no owning skill.** Three sessions included
   ad-hoc "what would quotas look like if..." modeling. The work maps to no
   existing big rock; partner planning keeps getting done as one-offs.

## Proposed edits

1. **`CLAUDE.md`** — add to Conventions:

   ```diff
   + - **Fiscal calendar:** Feb 1 – Jan 31. "FY26" = Feb 2025 – Jan 2026. Never
   +   assume calendar years for revenue figures.
   ```

2. **`.claude/skills/attribution-compare/SKILL.md`** — remove the embedded
   weight table; point at the plan as the single source of truth:

   ```diff
   - ## Model weights
   - | Model | Rule |
   - | Time Decay | weight ∝ 0.5 ^ (days / 45) ... |
   + Owned by big rock: plans/big-rocks/partner-attribution.md — that plan
   + holds the **canonical model weights**. Read it first; never restate or
   + invent weights here.
   ```

   (Canonical half-life confirmed as 30 days; the plan's tuning milestone
   stays open.)

3. **New skill proposal: `/quota-scenario`** — generate partner quota
   scenarios under configurable growth assumptions, reading
   `partner_metrics.csv`. Too large to build inside a retro; proposed under a
   new `partner-planning` big rock instead.

## Outcome

- Edit 1: **applied** — logged in `partner-scorecard.md` Improvement Log
  (the friction surfaced in scorecard sessions).
- Edit 2: **applied** — logged in `partner-attribution.md` Improvement Log.
- Edit 3: **deferred** — created `plans/big-rocks/partner-planning.md`
  (status: planned) with `/quota-scenario` as its first proposed skill, per
  the operating model: skills are proposed in a rock's plan before being built.
- `retros/session-log.md` trimmed to post-2026-06-05 entries.
